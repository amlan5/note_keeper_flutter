import 'package:flutter/material.dart';

class Create_qr extends StatelessWidget {
  const Create_qr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.black38,
          child: Center(child: Text("Create",style: TextStyle(fontSize: 60),),)),
    );
  }
}
